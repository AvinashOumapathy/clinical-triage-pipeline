An automated ETL system built with Make.com. It processes unstructured data from Tally forms, 
runs it through the Google Gemini API to extract key severity markers, and organizes it into a clean, 
structured JSON format for clinical reporting.
